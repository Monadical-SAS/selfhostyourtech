
# Base Configuration
BASE_DOMAIN=example.com
ENABLE_TRAEFIK=true
TRAEFIK_NETWORK=traefik-public

# HTTPS Configuration
HTTPS_ENTRYPOINT=websecure
ENABLE_TLS=true
TLS_RESOLVER=letsencrypt
HTTPS_MIDDLEWARE=redirect-to-https
REDIRECT_SCHEME=https
REDIRECT_PERMANENT=true

# Gitea Configuration
GITEA_POSTGRES_USER=gitea
GITEA_POSTGRES_PASSWORD=changeme_gitea_password
GITEA_POSTGRES_DB=gitea

# LibreChat Configuration
LIBRECHAT_PORT=3080
RAG_PORT=8000
MEILI_MASTER_KEY=changeme_meili_key
UID=1000
GID=1000

# Matomo Configuration
MATOMO_MYSQL_ROOT_PASSWORD=changeme_matomo_root_password
MATOMO_MYSQL_USER=matomo
MATOMO_MYSQL_PASSWORD=changeme_matomo_password
MATOMO_MYSQL_DATABASE=matomo

# WordPress Configuration
WORDPRESS_MYSQL_ROOT_PASSWORD=changeme_wordpress_root_password
WORDPRESS_MYSQL_USER=wordpress
WORDPRESS_MYSQL_PASSWORD=changeme_wordpress_password
WORDPRESS_MYSQL_DATABASE=wordpress
